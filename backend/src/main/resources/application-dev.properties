# Dev: Flyway migrates on startup; Hibernate may add minor entity drift only
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# If the DB was wiped but flyway_schema_history remains, repair then migrate
spring.flyway.validate-on-migrate=true
